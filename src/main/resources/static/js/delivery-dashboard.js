let orders=[
    {id:'JB-10245',tracking:'SD-884291',customer:'Nimali Perera',address:'18 Flower Road, Colombo 07',items:'AuraSound Pro Wireless Earbuds',expected:'Today, 4:30 PM',status:'out',pay:12.5},
    {id:'JB-10244',tracking:'PP-550182',customer:'Kasun Ranasinghe',address:'42 Lake View, Kandy',items:'ZenWatch 3 - Minimalist Smart Watch',expected:'Today, 6:00 PM',status:'shipped',pay:18},
    {id:'JB-10243',tracking:'CR-209441',customer:'Amara Silva',address:'9 Palm Grove, Galle',items:'MicroHub 8-in-1 USB-C Dock x 2',expected:'Tomorrow',status:'processing',pay:11.5},
    {id:'JB-10242',tracking:'SD-884270',customer:'Dinuk Wijesinghe',address:'71 Temple Lane, Negombo',items:'SlimPad Ultra Tablet 11 inch',expected:'Tomorrow',status:'processing',pay:22},
    {id:'JB-10241',tracking:'PP-550161',customer:'Shanika Fernando',address:'5 Station Road, Matara',items:'FocusDesk Lamp - Wireless Charging',expected:'Sep 21, 2026',status:'delivered',pay:10},
    {id:'JB-10240',tracking:'CR-209420',customer:'Ravi Mendis',address:'13 Park Avenue, Colombo 05',items:'AuraSound Pro Wireless Earbuds x 2',expected:'Sep 20, 2026',status:'delivered',pay:16.5}
];
const statusLabels={processing:'Processing',shipped:'Shipped',out:'Out for delivery',delivered:'Delivered',cancelled:'Cancelled'};
const notices=[
    {id:'JB-10245',icon:'!',title:'Order needs delivery',text:'Nimali Perera is waiting for her order. Deliver by 4:30 PM.',time:'5 min ago',unread:true},
    {id:'JB-10244',icon:'+',title:'Order ready for pickup',text:'Collect Kasun Ranasinghe\'s order and deliver it by 6:00 PM.',time:'18 min ago',unread:true},
    {id:'JB-10243',icon:'↗',title:'New delivery assigned',text:'A new order from Amara Silva was added to your route.',time:'42 min ago',unread:true},
    {id:'JB-10241',icon:'✓',title:'Delivery completed',text:'You completed Shanika Fernando\'s delivery. $10.00 was added.',time:'Yesterday',unread:false}
];
const $=selector=>document.querySelector(selector);const money=value=>`$${Number(value).toFixed(2)}`;
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
let query='';let filter='all';let route='dashboard';
function toast(text){const node=document.createElement('div');node.className='toast';node.textContent=text;$('#toast-stack').append(node);setTimeout(()=>node.remove(),2600)}
function activeOrders(){return orders.filter(order=>['processing','shipped','out'].includes(order.status))}
function renderStats(){const active=activeOrders().length;$('#order-badge').textContent=active;return `<div class="grid stats-grid"><article class="stat-card"><span class="stat-change">+12.5%</span><span class="stat-icon">↗</span><span class="stat-label">Total assigned orders</span><strong class="stat-value">128</strong></article><article class="stat-card"><span class="stat-change">+8.4%</span><span class="stat-icon">✓</span><span class="stat-label">Completed orders</span><strong class="stat-value">${96+orders.filter(x=>x.status==='delivered').length}</strong></article><article class="stat-card"><span class="stat-change">Needs attention</span><span class="stat-icon">!</span><span class="stat-label">Current orders</span><strong class="stat-value">${active}</strong></article><article class="stat-card"><span class="stat-change">+14.2%</span><span class="stat-icon">$</span><span class="stat-label">Total earnings</span><strong class="stat-value">$2,846</strong></article></div>`}
function renderDashboard(){const completed=orders.filter(x=>x.status==='delivered');return `${renderStats()}<div class="grid two-col"><article class="card"><div class="card-header"><div><h3>Delivery performance</h3><p>This month</p></div><span class="positive">+8.4%</span></div><div class="score-row"><strong class="score">94<span>%</span></strong><div class="score-copy"><strong>Excellent performance</strong><small>Above your 90% target</small><div class="progress"><i style="width:94%"></i></div></div></div><div class="legend"><span><i></i>On time <b>94%</b></span><span><i style="background:var(--dd-orange)"></i>Late <b>6%</b></span></div></article><article class="card"><div class="card-header"><div><h3>Orders by status</h3><p>Your current workload</p></div></div><div class="bar-list">${[['Processing','processing'],['Shipped','shipped'],['Out for delivery','out'],['Completed','delivered']].map(([label,key])=>{const count=key==='delivered'?92+completed.length:orders.filter(x=>x.status===key).length;return `<div class="bar-line"><strong>${label}</strong><div class="bar"><i style="width:${key==='delivered'?78:Math.max(8,count/6*100)}%"></i></div><span>${count}</span></div>`}).join('')}</div></article></div><div class="section-row"><div><h2>Today at a glance</h2><p>Your route and earnings summary.</p></div><button class="outline-button" data-go="orders">View current orders</button></div><div class="grid glance-grid"><article class="card glance"><span class="glance-icon">⌖</span><label>Stops completed today</label><strong>12 <small>/ 15 stops</small></strong><div class="progress"><i style="width:80%"></i></div></article><article class="card glance"><span class="glance-icon">$</span><label>Earned today</label><strong>$86.50</strong><small>+ $12.00 tips</small></article><article class="card glance"><span class="glance-icon">◷</span><label>Time on route</label><strong>5h 24m</strong><small>Estimated 1h 10m left</small></article><article class="card glance"><span class="glance-icon">★</span><label>Completed orders</label><strong>96 <small>this month</small></strong><small>Top 10% of drivers</small></article></div><div class="section-row"><div><h2>Recent activity</h2><p>Latest completed deliveries and payments.</p></div></div><div class="grid activity-grid"><article class="card"><div class="card-header"><h3>Recent completed orders</h3><button class="action-button" data-go="orders">View all</button></div>${completed.slice(0,3).map(x=>`<div class="activity"><div><strong>${x.id} - ${esc(x.customer)}</strong><span>${esc(x.items)}</span></div><b class="positive">Completed</b></div>`).join('')}</article><article class="card"><div class="card-header"><h3>Recent earnings</h3><button class="action-button" data-go="finance">Finance</button></div>${completed.slice(0,3).map(x=>`<div class="activity"><div><strong>${x.id}</strong><span>${x.expected}</span></div><b class="positive">+ ${money(x.pay)}</b></div>`).join('')}</article></div>`}
function matches(order){const text=`${order.id} ${order.tracking} ${order.customer} ${order.address} ${order.items}`.toLowerCase();return(filter==='all'||order.status===filter)&&(!query||text.includes(query.toLowerCase()))}
function renderOrders(){const list=orders.filter(matches);return `<div class="section-row"><div><h2>Current orders</h2><p>Accept, update, view, or remove orders assigned to you.</p></div><span class="positive">● Live queue</span></div><div class="toolbar"><label class="search-control">⌕<input id="order-query" value="${esc(query)}" placeholder="Search order or customer"></label><select id="status-filter" class="select"><option value="all">All statuses</option>${['processing','shipped','out'].map(x=>`<option value="${x}" ${filter===x?'selected':''}>${statusLabels[x]}</option>`).join('')}</select></div><article class="card table-wrap"><table class="order-table"><thead><tr><th>Order</th><th>Customer</th><th>Drop-off</th><th>Expected</th><th>Status</th><th>Pay</th><th>Actions</th></tr></thead><tbody>${list.map(x=>`<tr><td><strong>${x.id}</strong><small>${x.tracking}</small></td><td><strong>${esc(x.customer)}</strong></td><td><strong>${esc(x.items)}</strong><small>${esc(x.address)}</small></td><td>${x.expected}</td><td><span class="status ${x.status}">${statusLabels[x.status]}</span></td><td><strong>${money(x.pay)}</strong></td><td><button class="action-button" data-view="${x.id}">View</button> <button class="action-button" data-delete="${x.id}">Delete</button></td></tr>`).join('')}</tbody></table>${list.length?'':'<div class="empty">No current orders found.</div>'}</article>`}
function renderDelivery(){return `<div class="section-row"><div><h2>Delivery route</h2><p>Track every active delivery on your route.</p></div><span class="positive">${activeOrders().length} active deliveries</span></div><div class="grid route-grid"><article class="card"><div class="fake-map"><span class="map-pin pin-a">A</span><span class="map-pin pin-b">B</span><span class="map-pin pin-c">C</span><div class="map-line"></div><div class="map-center">+</div><small class="map-center">Your current position</small></div><div class="map-caption"><strong>Colombo city route</strong><span>12.6 km remaining - 1h 10m</span></div></article><article class="card"><div class="card-header"><div><h3>Next stops</h3><p>Optimized order</p></div></div>${activeOrders().slice(0,4).map((x,index)=>`<div class="stop"><span class="stop-number">${index+1}</span><div><strong>${esc(x.customer)}</strong><span>${esc(x.address)}</span></div><time>${x.expected.replace(', 2026','')}</time></div>`).join('')}</article></div>`}
function renderFinance(){const total=orders.reduce((sum,x)=>sum+x.pay,2750);return `<div class="section-row"><div><h2>Driver finance</h2><p>Track your delivery earnings, tips, and payouts.</p></div><button id="payout" class="primary-button">Request payout</button></div><div class="grid finance-stats"><article class="card finance-stat"><span>Total earnings</span><strong>${money(total)}</strong><small>This month</small></article><article class="card finance-stat"><span>Delivery pay</span><strong>$2,410.50</strong><small>+12.8%</small></article><article class="card finance-stat"><span>Tips received</span><strong>$436.00</strong><small>+18.4%</small></article></div><div class="grid finance-grid"><article class="card"><div class="card-header"><div><h3>Earnings history</h3><p>Recent completed delivery payments</p></div></div>${orders.filter(x=>x.status==='delivered').map(x=>`<div class="activity"><div><strong>${x.id}</strong><span>${x.expected} - Delivered</span></div><b class="positive">+ ${money(x.pay)}</b></div>`).join('')}</article><article class="card"><div class="card-header"><div><h3>Next payout</h3><p>Scheduled weekly transfer</p></div><span class="status shipped">Friday</span></div><strong class="payout-amount">$486.20</strong><p>Available balance</p><div class="payout-row"><span>Base delivery pay</span><strong>$418.20</strong></div><div class="payout-row"><span>Customer tips</span><strong>$68.00</strong></div><div class="payout-row"><span>Total this period</span><strong>$486.20</strong></div><button id="payout-history" class="outline-button full">View payout history</button></article></div>`}
function renderSettings(){return `<div class="section-row"><div><h2>Driver profile and settings</h2><p>Manage your identity, availability, security, and payout preferences.</p></div></div><div class="grid settings-grid"><article class="card"><div class="card-header"><div><h3>Driver profile</h3><p>Shown to customers when you are on route.</p></div></div><form id="profile-form"><label class="field"><span>Full name</span><input name="name" value="Alex Silva" required></label><label class="field"><span>Vehicle number</span><input name="vehicle" value="WP CAB-4821" required></label><button class="primary-button">Save profile</button></form></article><article class="card"><h3>Work preferences</h3>${[['Available for new orders','Receive new delivery assignments',true],['Auto-accept nearby orders','Accept orders within 5 km automatically',false],['Delivery reminders','Notify me 30 minutes before due time',true]].map(([title,text,on])=>`<label class="setting-row"><span><strong>${title}</strong><small>${text}</small></span><button type="button" class="toggle ${on?'on':''}" aria-checked="${on}"><i></i></button></label>`).join('')}</article><article class="card"><h3>Security and payout</h3><div class="setting-row"><span><strong>Payment method</strong><small>Commercial Bank - 4821</small></span></div><div class="setting-row"><span><strong>Payout schedule</strong><small>Weekly - Every Friday</small></span></div><button id="change-password" class="outline-button">Change password</button></article></div>`}
function renderNotifications(){const unread=notices.filter(x=>x.unread).length;$('#notification-badge').textContent=unread;$('#top-notification-badge').textContent=unread;return `<div class="section-row"><div><h2>Notifications</h2><p>Orders and updates that need your attention.</p></div><button id="mark-read" class="outline-button">Mark all as read</button></div><div class="grid notification-grid"><article class="card">${notices.map(n=>`<div class="notification"><span class="notification-icon">${n.icon}</span><div><strong>${n.title}</strong><p>${n.text}</p><time>${n.time}</time></div><button class="action-button" data-notice="${n.id}">View</button></div>`).join('')}</article><article class="card"><span class="glance-icon">!</span><h3 style="margin-top:18px">Stay on schedule</h3><p>Alerts show orders you still need to collect or deliver. Open an order to view its address and update the status after completing the stop.</p></article></div>`}
function openOrder(order){$('#modal').innerHTML=`<h2>${order.id}</h2><p>${order.tracking} - ${esc(order.items)}</p><div class="payout-row"><span>Customer</span><strong>${esc(order.customer)}</strong></div><div class="payout-row"><span>Drop-off</span><strong>${esc(order.address)}</strong></div><div class="payout-row"><span>Your earning</span><strong>${money(order.pay)}</strong></div><div class="modal-actions"><button id="close-modal" class="outline-button">Close</button><button id="update-order" class="primary-button">Update status</button></div>`;$('#modal-layer').hidden=false;$('#close-modal').onclick=closeModal;$('#update-order').onclick=()=>{const next={processing:'shipped',shipped:'out',out:'delivered',delivered:'delivered',cancelled:'processing'}[order.status];order.status=next;closeModal();render();toast(`${order.id} updated to ${statusLabels[next]}`)}}
function closeModal(){$('#modal-layer').hidden=true}
function bindView(){document.querySelectorAll('[data-go]').forEach(button=>button.onclick=()=>location.hash=button.dataset.go);$('#order-query')?.addEventListener('input',event=>{query=event.target.value;render()});$('#status-filter')?.addEventListener('change',event=>{filter=event.target.value;render()});document.querySelectorAll('[data-view]').forEach(button=>button.onclick=()=>{const order=orders.find(x=>x.id===button.dataset.view);if(order)openOrder(order)});document.querySelectorAll('[data-delete]').forEach(button=>button.onclick=()=>{const index=orders.findIndex(x=>x.id===button.dataset.delete);if(index>-1&&confirm(`Remove ${orders[index].id} from your current orders?`)){orders.splice(index,1);render();toast('Order removed')}});document.querySelectorAll('[data-notice]').forEach(button=>button.onclick=()=>{const notice=notices.find(x=>x.id===button.dataset.notice);if(notice){notice.unread=false;location.hash='orders';const order=orders.find(x=>x.id===notice.id);if(order)setTimeout(()=>openOrder(order),0)}});$('#mark-read')?.addEventListener('click',()=>{notices.forEach(x=>x.unread=false);render();toast('All notifications marked as read')});$('#payout')?.addEventListener('click',()=>toast('Payout request submitted for review'));$('#payout-history')?.addEventListener('click',()=>toast('Payout history opened'));$('#profile-form')?.addEventListener('submit',event=>{event.preventDefault();toast('Profile updated successfully')});document.querySelectorAll('.setting-row .toggle').forEach(button=>button.onclick=()=>{const on=button.getAttribute('aria-checked')!=='true';button.setAttribute('aria-checked',on);button.classList.toggle('on',on);toast(on?'Setting enabled':'Setting disabled')});$('#change-password')?.addEventListener('click',()=>{$('#modal').innerHTML='<h2>Change password</h2><p>Choose a new password for your driver account.</p><form id="password-form"><label class="field"><span>New password</span><input name="password" type="password" minlength="6" required></label><label class="field"><span>Confirm password</span><input name="confirm" type="password" minlength="6" required></label><div class="modal-actions"><button type="button" id="close-modal" class="outline-button">Cancel</button><button class="primary-button">Save password</button></div></form>';$('#modal-layer').hidden=false;$('#close-modal').onclick=closeModal;$('#password-form').onsubmit=event=>{event.preventDefault();const values=new FormData(event.target);if(values.get('password')!==values.get('confirm')){toast('Passwords do not match');return}closeModal();toast('Password changed successfully')}})}
const meta={dashboard:['Dashboard','Good morning, Alex','Here is your delivery progress for today.'],orders:['Orders','Current orders','Manage the orders assigned to your route.'],delivery:['Delivery','Your delivery route','Follow your stops and keep customers updated.'],finance:['Finance','Driver earnings','Track your delivery pay, tips, and payouts.'],settings:['Settings','Driver profile and settings','Manage your driver account and preferences.'],notifications:['Notifications','Delivery alerts','Orders and updates that need your attention.']};
function render(){route=location.hash.slice(1)||'dashboard';if(!meta[route])route='dashboard';document.querySelectorAll('.view').forEach(view=>view.hidden=view.id!==`${route}-view`);document.querySelectorAll('[data-route]').forEach(link=>link.classList.toggle('active',link.dataset.route===route));$('#route-title').textContent=meta[route][0];$('#greeting').textContent=meta[route][1];$('#subtitle').textContent=meta[route][2];const views={dashboard:renderDashboard,orders:renderOrders,delivery:renderDelivery,finance:renderFinance,settings:renderSettings,notifications:renderNotifications};$(`#${route}-view`).innerHTML=views[route]();bindView();closeMenu()}
function closeMenu(){$('#driver-sidebar').classList.remove('open');$('#backdrop').classList.remove('open')}
function init(){const session=JSON.parse(localStorage.getItem('jb_user')||'null');if(!session||session.role!=='driver'){window.location.replace('/HTML/login.html');return}$('#today').textContent=new Date().toLocaleDateString('en-US',{weekday:'long',month:'long',day:'numeric',year:'numeric'});document.querySelectorAll('[data-route]').forEach(link=>link.onclick=event=>{event.preventDefault();location.hash=link.dataset.route});$('#global-search').onsubmit=event=>{event.preventDefault();query=$('#global-query').value.trim();location.hash='orders'};$('#notifications').onclick=()=>location.hash='notifications';$('#profile').onclick=()=>location.hash='settings';$('#export').onclick=()=>toast('Earnings report prepared for download');$('#theme').onclick=()=>{document.body.classList.toggle('dark');localStorage.setItem('jb_driver_theme',document.body.classList.contains('dark')?'dark':'light')};if(localStorage.getItem('jb_driver_theme')==='dark')document.body.classList.add('dark');$('#availability').onclick=event=>{const button=event.currentTarget;const on=button.getAttribute('aria-checked')!=='true';button.setAttribute('aria-checked',on);button.classList.toggle('on',on);toast(on?'You are available for new orders':'You are now offline')};$('#logout').onclick=()=>{localStorage.removeItem('jb_user');localStorage.removeItem('jb_seller');window.location.href='/HTML/login.html'};$('#menu').onclick=()=>{$('#driver-sidebar').classList.add('open');$('#backdrop').classList.add('open')};$('#backdrop').onclick=closeMenu;$('#modal-layer').onclick=event=>{if(event.target.id==='modal-layer')closeModal()};window.addEventListener('hashchange',render);render()}
document.addEventListener('DOMContentLoaded',init);
async function loadLiveDriverAnalytics(){const session=JSON.parse(localStorage.getItem('jb_user')||'null');if(!session?.id)return;try{const response=await fetch(`/api/driver/${encodeURIComponent(session.id)}/analytics`);if(!response.ok)return;const data=await response.json();if(Array.isArray(data.orders)&&data.orders.length){orders=data.orders.map(x=>({id:x.orderNumber||`JB-${x.id}`,backendId:x.id,tracking:x.trackingNumber||'',customer:x.customerName||'Customer',address:x.shippingAddress||'',items:(x.items||[]).map(i=>`${i.productName||'Product'} x${i.quantity||1}`).join(', '),expected:x.estimatedDelivery||'—',status:String(x.status||'PENDING').toLowerCase().replace('out_for_delivery','out'),pay:Number(x.total||0)*.05}))}const dashboard=document.getElementById('dashboard-view');if(!dashboard)return;const panel=document.createElement('section');panel.className='driver-live-analytics';panel.innerHTML=`<h3>Live database overview</h3><div class="driver-live-grid"><strong>${data.totalOrders}</strong><span>Total orders</span><strong>${data.inTransit}</strong><span>In transit</span><strong>${data.deliveredOrders}</strong><span>Delivered</span><strong>$${Number(data.earnings||0).toFixed(2)}</strong><span>Estimated earnings</span></div>`;dashboard.prepend(panel);render()}catch(error){console.warn('Driver analytics unavailable',error)}}
document.addEventListener('DOMContentLoaded',()=>setTimeout(loadLiveDriverAnalytics,0));
